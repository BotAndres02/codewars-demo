/**
 * Write a program that displays the multiplication table from 1 to 12.
 */

let x = 9;
let y = 1;

while (y <= 12) {
    result = (x * y)
    console.log( x+ " * " + y +" = "+ result);
    y++;
}