/**
 * Write a program that prompts the user for a positive 
 * integer and displays all the odd numbers from 1 to 
 * any number separated by commas.
 */

var numero = 5;
    for (let i = 1; i <= numero; i+=2) {
        console.log(i + ",");
    }