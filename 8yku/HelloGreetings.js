/**
 * Write a two-line program that prompts for 
 * the user's name with a prompt and writes 
 * a text that says "Hello User name".
 */

let name = prompt("Ingrese un nombre")
document.write("Hola " + name);